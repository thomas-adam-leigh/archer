node_modules
.DS_Store
dist
dist-ssr
*.local
.env
# Track the dummy PUBLIC e2e build config (no secrets) — the root .gitignore's
# `.env.*` rule would otherwise exclude it; the Cypress web-e2e build needs it.
!.env.e2e
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Cypress run artifacts (uploaded as CI artifacts on failure, never committed)
cypress/videos
cypress/screenshots
cypress/downloads
